name: paulette
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  flutter_map: ^8.2.2   # Revisa la última versión en pub.dev
  table_calendar: ^3.0.9 #CALENDARIO
  intl: ^0.20.2
  image_picker: ^1.2.1
  flutter_image_compress: ^2.4.0
  uuid: ^4.3.3
  shared_preferences: ^2.2.2
  url_launcher: ^6.3.2
  font_awesome_flutter: ^10.7.0

  flutter_localizations:
    sdk: flutter
    #dependencias
  firebase_core: ^4.2.1
  cloud_firestore: ^6.1.0
  firebase_auth: ^6.1.2
  firebase_storage: ^13.0.4

  #dependencias para cloudinary
  crypto: ^3.0.3
  latlong2: ^0.9.0
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
   - assets/images/
